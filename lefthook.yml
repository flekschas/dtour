pre-commit:
  parallel: true
  commands:
    biome:
      glob: "*.{js,ts,jsx,tsx,json,jsonc}"
      run: pnpm biome check --no-errors-on-unmatched {staged_files}
    ruff:
      glob: "*.py"
      run: uvx ruff check --config packages/python/pyproject.toml {staged_files}
